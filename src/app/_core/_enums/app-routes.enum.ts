export enum eAppRoutes {
  PRINCIPAL = '',
  HOME = 'inicio',
  LOGIN = 'iniciar-sesion',
  HOTEL = 'gestion-hotel',
  RESERVATION = 'reservacion',
  RESERVATION_CREATE = 'crear-reservacion/:hotelId/:roomId',
  RESERVATION_DETAIL = 'detalle-reservaciones'
};
